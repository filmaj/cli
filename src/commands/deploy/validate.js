module.exports = function validate(/*opts*/) {
  // check for AWS_REGION and AWS_PROFILE
  // check for .aws/credentials
  // check for sam-cli
  // check for aws-cli
  // check for .arc
  // check for arc.aws.bucket
}
