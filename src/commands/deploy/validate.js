//deploy by default to AppNameStaging
//deploy production deploys to AppNameProduction
//use `arc package` if you want to do other things

module.exports = function validate(/*opts*/) {
  // check for AWS_REGION and AWS_PROFILE
  // check for sam
  // check for aws-cli ?
  // check for .arc
  // check for arc.aws.bucket
}
